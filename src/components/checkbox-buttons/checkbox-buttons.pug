include ../checkbox-button/checkbox-button.pug
mixin checkboxButtons({label,list,name})
  .checkbox-buttons
    span.checkbox-buttons__label #{label}
    ul.checkbox-buttons__list
      each val in list
        li.checkbox-buttons__option
          +checkbox-button({text:val,name})

